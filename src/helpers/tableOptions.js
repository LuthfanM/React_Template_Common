const minFeature = {
    filterType: "dropdown",
    responsive: "scroll",
    filter: true,
    download: false,
    print: false
};

const defaultFeature = {
    filter: true,
    filterType: "dropdown",
    responsive: "standard"
};

const standardFeature = {
    filter: true,
    filterType: 'dropdown',
    responsive: 'standard'
};

export {minFeature, defaultFeature, standardFeature}